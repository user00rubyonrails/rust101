building todo-app
- todo structs
  main.rs
  to_do/
    mod.rs
    structs/
      base.rs
      done.rs
      pending.rs
      mod.rs


## Need scalable: why?
other dev might also implement our mod incorrectly.
Prevent it! use `interface`
use factory pattern.

## Need to defined `CRUD`: we need to give our structs some `traits`.
- defining a trait for each type of process gives us the ability to fine-tune the functionality for each type